package com.example.booklog.common.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // 기본 설정을 사용 - SpringDoc이 자동으로 API 문서를 생성합니다
    // 접속 문제는 나중에 해결하고 일단 컴파일이 되도록 함
}
